<%= form_for(current_user.gamers.
                          build(going_game_id: @game.id),remote: true) do |f| %>
  <div><%= hidden_field_tag :going_game_id, @game.id %></div>
  <%= f.submit "Going", class: "btn btn-primary" %>
<% end %>